#include <stdio.h>
#include <ctype.h>
#include <cs50.h>

int main (int argc, string argv[])
{
    // Error check: argument must be a positive integer.
    // For each char in arg string, check if digit
     
    for(int i = 0, int length = strlen(argv[1]); i < length; i++)
    {
        
    }
}
